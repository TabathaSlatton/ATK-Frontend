class CartProduc{
  constructor(name, username, email){
    this.name = name,
    this.username = username,
    this.email = email
  }
}

// create fetch request

// read fetch request

// delete fetch request